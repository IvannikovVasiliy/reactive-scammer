server:
  port: 8081

spring:
  kafka:
    bootstrapAddress: broker-analyzer:29092
    topic:
      payments: payments
      suspicious-payments: suspicious-payments
      checked-payments: checked-payments
      backoff-payments: backoff-payments
    consumer:
      groupId: analyzer-group-id

redis:
  hostName: redis-analyzer
  port: 6379

hostPort:
  paymentService: http://payment-service:8082

app:
  durationPollMillis: 500
  signForHandleBackoffPayment: 10000
  retryCount: 3
  retryIntervalSeconds: 3
  redisTimeout: 10

logging:
  level:
    ru.neoflex.scammertracking.analyzer: info